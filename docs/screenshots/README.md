# Screenshots

Place final product screenshots for the CLI here.

Recommended files:
- `cli-doctor.png`
- `cli-report.png`
- `cli-remote.png`

Suggested capture style:
- use a clean terminal theme
- crop tightly around the important output
- avoid showing private tokens, database IDs, or local secrets
